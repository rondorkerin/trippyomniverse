'use client'

// ModuleRenderer is no longer used — chunk rendering is handled by
// ChunkRenderer in Universe.tsx. This file is kept as a no-op for
// any lingering imports during the transition.

export default function ModuleRenderer() {
  return null
}
