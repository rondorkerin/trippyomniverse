import type { ModuleManifest } from '@/core/types'

const manifest: ModuleManifest = {
  name: 'audio-visualizer',
  description: 'FFT frequency bars and waveform ring that react to microphone or system audio input',
  version: '1.0.0',
  tags: ['audio', 'reactive', 'core'],
}

export default manifest
