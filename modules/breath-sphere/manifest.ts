import type { ModuleManifest } from '@/core/types'

const manifest: ModuleManifest = {
  name: 'breath-sphere',
  description: 'A 3D sphere that pulses with breathing rhythm — inhale 4s, hold 2s, exhale 6s',
  version: '1.0.0',
  tags: ['breath', 'meditation', 'core'],
}

export default manifest
