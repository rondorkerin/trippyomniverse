import type { ModuleManifest } from '@/core/types'

const manifest: ModuleManifest = {
  name: 'fractal-web',
  description: 'Recursive fractal lines that branch and breathe, creating a web of light',
  version: '1.0.0',
  tags: ['fractal', 'geometry', 'core'],
}

export default manifest
