import type { ModuleManifest } from '@/core/types'

const manifest: ModuleManifest = {
  name: 'kaleidoscope',
  description: 'Shader-based kaleidoscope with webcam or procedural fallback',
  version: '1.0.0',
}

export default manifest
