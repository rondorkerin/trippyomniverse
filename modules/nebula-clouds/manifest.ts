import type { ModuleManifest } from '@/core/types'

const manifest: ModuleManifest = {
  name: 'nebula-clouds',
  description: 'Deep space particle nebula with organic cloud clusters',
  version: '1.0.0',
}

export default manifest
