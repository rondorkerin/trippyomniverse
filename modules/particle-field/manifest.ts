import type { ModuleManifest } from '@/core/types'

const manifest: ModuleManifest = {
  name: 'particle-field',
  description: '2500 particles in a breathing sphere that react to mouse position with HSL color cycling',
  version: '1.0.0',
  tags: ['particles', 'core'],
}

export default manifest
