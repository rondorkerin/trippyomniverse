import type { ModuleManifest } from '@/core/types'

const manifest: ModuleManifest = {
  name: 'sacred-rings',
  description: 'Concentric torus rings that rotate at different speeds, breathing and shifting with input',
  version: '1.0.0',
  tags: ['geometry', 'sacred', 'core'],
}

export default manifest
